$(document).ready(function() {
    $("#flipbook").turn({
        width: 800,  // Set book width
        height: 600, // Set book height
        autoCenter: true, // Center book in viewport
        acceleration: true, // Enable smooth flipping
    });
});
